extends /layouts/application.pug
block content
  form(action=routePath('sessions') method="post")
    div
      label Email
      input(type="text" name="user[email]")
    div
      label Password
      input(type="password" name="user[password]")
    div
      input(type="submit")

  a(href=routePath('root')) Main page

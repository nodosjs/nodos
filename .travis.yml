---

language: node_js
node_js:
  - "12.10.0"

sudo: false

cache:
  directories:
    - "node_modules"

before_install:
  - npx lerna bootstrap

before_script:
  - make build

script:
  - make lint-ci
  - make test


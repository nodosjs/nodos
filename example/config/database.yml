---

default: &default
  type: postgres
  host: db
  username: postgres
  # encoding: unicode

development:
  <<: *default
  type: sqlite
  database: db/development.sqlite3

test:
  <<: *default
  type: postgres
  database: example_test

# production:
#   <<: *default
#   url: <%= ENV['DATABASE_URL'] %>
